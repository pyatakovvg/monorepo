export enum ESize {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
}

export enum EMode {
  PRIMARY = 'primary',
  SUCCESS = 'success',
  DANGER = 'danger',
}

export enum EAlign {
  LEFT = 'left',
  RIGHT = 'right',
  CENTER = 'center',
}
