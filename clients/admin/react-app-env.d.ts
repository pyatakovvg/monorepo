/// <reference types="react" />
/// <reference types="react-scripts" />


interface Window {
  env: {
    API_HOST: string;
    ATTEMPT_WAIT: number;
    ATTEMPT_COUNT: number;
    HIDE_CARD_NUMBER_DELAY: number;
  }
}
